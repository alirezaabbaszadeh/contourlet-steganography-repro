{
  "schema_version": 1,
  "protocol_id": "FINAL-5J-v1",
  "status": "locked_before_performance_results",
  "profile_id": "ferdowsi-8c16g-100gb-v2",
  "target_host": {
    "logical_cpus": 8,
    "memory_gib": 16,
    "minimum_memory_gib": 14,
    "storage_gb_decimal": 100,
    "minimum_total_storage_gib": 90,
    "reserved_cpus": 1,
    "minimum_memory_headroom_gib": 3.5,
    "absolute_minimum_available_memory_gib": 3,
    "minimum_free_storage_gib": 30
  },
  "candidate_order": [
    4,
    2,
    1,
    6,
    7
  ],
  "fallback_workers": [
    2,
    1
  ],
  "scale_up_workers": [
    6,
    7
  ],
  "initial_workers": 4,
  "maximum_workers": 7,
  "worker_threads": 1,
  "minimum_workload": {
    "embedding_tasks": 32,
    "evaluation_tasks": 128,
    "internal_methods": [
      "C0",
      "C1",
      "C2",
      "C3_NP",
      "C3"
    ],
    "minimum_payload_fraction_count": 2,
    "required_attack_families": [
      "clean",
      "jpeg",
      "gaussian",
      "salt_pepper"
    ]
  },
  "rejection_thresholds": {
    "maximum_operational_failures": 0,
    "maximum_oom_events": 0,
    "maximum_swap_io_bytes": 0,
    "minimum_available_memory_gib": 3,
    "minimum_free_storage_gib": 30,
    "maximum_sustained_iowait_percent": 15
  },
  "scale_up_thresholds": {
    "minimum_mean_cpu_busy_percent": 70,
    "maximum_p95_iowait_percent": 10,
    "minimum_projected_memory_headroom_gib": 3.5,
    "minimum_gain_percent_for_6": 7.5,
    "minimum_gain_percent_for_7": 5.0
  },
  "stop_thresholds": {
    "minimum_incremental_throughput_gain_percent": 5,
    "maximum_repeat_throughput_difference_percent": 10
  },
  "notes": [
    "All workers remain single-threaded internally.",
    "The first measured trial is four workers on the 8-core host.",
    "One logical CPU is always reserved for the OS, monitoring, Octave coordination, and I/O.",
    "Six workers are tested only after a stable four-worker trial; seven workers are tested only after a stable, materially faster six-worker trial.",
    "A trial is unsafe after any operational failure, OOM event, swap I/O, memory-floor breach, storage-floor breach, or sustained I/O-wait breach.",
    "The selected worker count must be confirmed once with a fresh benchmark cache before scientific execution."
  ]
}
